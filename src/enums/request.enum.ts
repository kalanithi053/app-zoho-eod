export enum MapRequest {
  get = "GET",
  post = "POST",
  patch = "PATCH",
  put = "PUT",
  delete = "DELETE",
}

export enum HttpHeaders {
  ContentType = "Content-Type",
}

export enum MimeTypes {
  ApplicationJson = "application/json",
}
